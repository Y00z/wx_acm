
<!--pages/detail/detail.wxml--> 
<view>
    <scroll-view scroll-y="true" style="height: 100%;">
      <import src="../table/table.wxml" />
      <template is="data-table" data="{{...mData}}" />
    </scroll-view>
  <!--<view class="swiper-tab-list {{currentTab==0 ? 'on' : ''}}" data-current="0" bindtap="swichNav">表格</view>-->
  <!--<view class="swiper-tab-list {{currentTab==1 ? 'on' : ''}}" data-current="1" bindtap="swichNav">曲线</view>
  <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">仪表</view>-->
</view>
<!--<swiper current="{{currentTab}}" class="swiper-box" duration="300" style="height:{{winHeight - 31}}px" bindchange="bindChange">-->
  <!--<swiper-item>
    <scroll-view scroll-y="true" style="height: 100%;">
      <import src="../table/table.wxml" />
      <template is="data-table" data="{{...mData}}" />
    </scroll-view>
  </swiper-item>-->
  <!--<swiper-item>
    <view>曲线</view>
    <view>{{gid}}</view>
  </swiper-item>
  <swiper-item>
    <view>仪表</view>
  </swiper-item>-->
<!--</swiper>-->